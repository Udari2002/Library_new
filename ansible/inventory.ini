[webservers]
# AWS EC2 instance - IP will be provided by Terraform output
# Replace with actual EC2 public IP after terraform apply
aws-server ansible_host=REPLACE_WITH_EC2_IP ansible_user=ec2-user ansible_ssh_private_key_file=~/.ssh/library-app-key.pem
